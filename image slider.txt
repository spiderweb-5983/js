<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Image Slider</title>
    <style>
        body { font-family: Arial; background: #f0f0f0; text-align: center; }
      .slider {
            position: relative;
            width: 600px;
            margin: 50px auto;
            border: 5px solid white;
            box-shadow: 0 0 20px rgba(0,0,0,0.3);
        }
      .slider img { width: 100%; height: 400px; display: block; }
      .slider button {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            padding: 10px 15px;
            background: black;
            color: white;
            border: none;
            cursor: pointer;
        }
        #prevBtn { left: 0; }
        #nextBtn { right: 0; }
    </style>
</head>
<body>
    <h2>Image Slider</h2>
    <div class="slider">
        <img id="slide" src="https://picsum.photos/id/1015/600/400" alt="slider">
        <button id="prevBtn">Previous</button>
        <button id="nextBtn">Next</button>
    </div>
    <p id="status">Image 1 of 3</p>

<script>
    let images = [
        "https://picsum.photos/id/1015/600/400",
        "https://picsum.photos/id/1016/600/400",
        "https://picsum.photos/id/1018/600/400"
    ];
    let currentIndex = 0;
    let img = document.getElementById("slide");
    let status = document.getElementById("status");

    function showImage(index){
        img.src = images[index];
        status.textContent = `Image ${index + 1} of ${images.length} - Changing...`;
        console.log("Changed to", index);
    }

    document.getElementById("nextBtn").onclick = function(){
        currentIndex = (currentIndex + 1) % images.length;
        showImage(currentIndex);
    };

    document.getElementById("prevBtn").onclick = function(){
        currentIndex = (currentIndex - 1 + images.length) % images.length;
        showImage(currentIndex);
    };

    // Auto change
    /*setInterval(function(){
        currentIndex = (currentIndex + 1) % images.length;
        showImage(currentIndex);
    }, 3000);*/
</script>
</body>
</html>